---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: appfirewalls.xc.f5.com
spec:
  group: xc.f5.com
  names:
    kind: AppFirewall
    listKind: AppFirewallList
    plural: appfirewalls
    shortNames:
    - afw
    singular: appfirewall
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: Synced
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AppFirewall is the Schema for the appfirewalls API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              allowAllResponseCodes:
                description: 'Response codes — OneOf: AllowAllResponseCodes | AllowedResponseCodes'
                type: object
              allowedResponseCodes:
                properties:
                  responseCode:
                    items:
                      type: integer
                    type: array
                required:
                - responseCode
                type: object
              blocking:
                description: |-
                  EmptyObject is a named empty struct used as a sentinel/flag value in F5XC
                  "oneof" fields where the API expects an empty JSON object ("{}").
                type: object
              blockingPage:
                properties:
                  blockingPage:
                    type: string
                  responseCode:
                    type: string
                type: object
              botProtectionSetting:
                properties:
                  goodBotAction:
                    type: string
                  maliciousBotAction:
                    type: string
                  suspiciousBotAction:
                    type: string
                type: object
              customAnonymization:
                properties:
                  anonymizationConfig:
                    items:
                      properties:
                        cookieName:
                          type: string
                        headerName:
                          type: string
                        queryParameter:
                          type: string
                      type: object
                    type: array
                  specificDomains:
                    items:
                      type: string
                    type: array
                type: object
              defaultAnonymization:
                description: 'Anonymization — OneOf: DefaultAnonymization | DisableAnonymization
                  | CustomAnonymization'
                type: object
              defaultBotSetting:
                description: 'Bot setting — OneOf: DefaultBotSetting | BotProtectionSetting'
                type: object
              defaultDetectionSettings:
                description: 'Detection — OneOf: DefaultDetectionSettings | DetectionSettings'
                type: object
              detectionSettings:
                properties:
                  disableSuppression:
                    description: |-
                      EmptyObject is a named empty struct used as a sentinel/flag value in F5XC
                      "oneof" fields where the API expects an empty JSON object ("{}").
                    type: object
                  disableThreatCampaigns:
                    description: |-
                      EmptyObject is a named empty struct used as a sentinel/flag value in F5XC
                      "oneof" fields where the API expects an empty JSON object ("{}").
                    type: object
                  enableSuppression:
                    description: |-
                      EmptyObject is a named empty struct used as a sentinel/flag value in F5XC
                      "oneof" fields where the API expects an empty JSON object ("{}").
                    type: object
                  enableThreatCampaigns:
                    description: |-
                      EmptyObject is a named empty struct used as a sentinel/flag value in F5XC
                      "oneof" fields where the API expects an empty JSON object ("{}").
                    type: object
                  signatureSelectionSetting:
                    properties:
                      attackTypeSettings:
                        properties:
                          disabledAttackTypes:
                            items:
                              properties:
                                name:
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                        type: object
                      defaultAttackTypeSettings:
                        description: |-
                          EmptyObject is a named empty struct used as a sentinel/flag value in F5XC
                          "oneof" fields where the API expects an empty JSON object ("{}").
                        type: object
                      highMediumAccuracySignatures:
                        description: |-
                          EmptyObject is a named empty struct used as a sentinel/flag value in F5XC
                          "oneof" fields where the API expects an empty JSON object ("{}").
                        type: object
                      highMediumLowAccuracySignatures:
                        description: |-
                          EmptyObject is a named empty struct used as a sentinel/flag value in F5XC
                          "oneof" fields where the API expects an empty JSON object ("{}").
                        type: object
                      onlyHighAccuracySignatures:
                        description: |-
                          EmptyObject is a named empty struct used as a sentinel/flag value in F5XC
                          "oneof" fields where the API expects an empty JSON object ("{}").
                        type: object
                    type: object
                type: object
              disableAnonymization:
                description: |-
                  EmptyObject is a named empty struct used as a sentinel/flag value in F5XC
                  "oneof" fields where the API expects an empty JSON object ("{}").
                type: object
              monitoring:
                description: 'Enforcement mode — OneOf: Monitoring | Blocking'
                type: object
              useDefaultBlockingPage:
                description: 'Blocking page — OneOf: UseDefaultBlockingPage | BlockingPage'
                type: object
              xcNamespace:
                type: string
            required:
            - xcNamespace
            type: object
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
              xcNamespace:
                type: string
              xcResourceVersion:
                type: string
              xcUID:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
